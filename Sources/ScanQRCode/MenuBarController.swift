import AppKit

// STUB — owned by Agent B. Replace the entire file with the real
// NSStatusItem menu (two scan items, Settings…, Quit).
@MainActor
final class MenuBarController {
    func install() {}
}
