import Foundation

// STUB — owned by Agent A. Replace the entire file with the real
// Vision `VNDetectBarcodesRequest` implementation.
struct QRDetector: QRDetecting {
    func detect(in imageURL: URL) throws -> [String] { [] }
}
