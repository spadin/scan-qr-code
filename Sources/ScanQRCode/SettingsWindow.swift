import AppKit

// STUB — owned by Agent B. Replace the entire file with the real SwiftUI
// settings window (KeyboardShortcuts.Recorder per action).
@MainActor
final class SettingsWindowController {
    func show() {}
}
